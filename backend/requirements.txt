# CareGuide Backend — Python Dependencies

# Web framework
fastapi==0.139.2
uvicorn[standard]==0.51.0

# AI
anthropic==0.117.0
# OpenAI — the OpenAI side of the two-frontier A/B pair (Responses API). Only
# imported when a caller routes an OpenAI model id (baselines); everything else
# stays on Anthropic. Pinned to a version that ships client.responses.
openai==2.46.0

# HTTP client (ElevenLabs, Tavus)
httpx==0.28.1
aiofiles==25.1.0

# Data validation
pydantic==2.13.4

# SMS
twilio==9.10.9

# Email (SendGrid Web API)
sendgrid>=6.12.5

# PDF parsing
PyPDF2==3.0.1
pdfminer.six>=20260107
pdf2image>=1.17.0
pytesseract>=0.3.13
Pillow>=12.3.0

# File uploads
python-multipart>=0.0.32

# Environment config
python-dotenv==1.2.2

# Auth (landing / Elysium Health)
# PyJWT replaces python-jose, which carried CVE-2024-33663 (algorithm confusion)
# and CVE-2024-33664 (DoS). HS256 only; algorithms are pinned at every decode.
# >=2.13.0 also clears the newer PyJWT advisories (PYSEC-2026-175/177/179).
PyJWT>=2.13.0
passlib[bcrypt]==1.7.4
email-validator>=2.3.0
# MFA (TOTP) — PRD-3
pyotp>=2.10.0
# Field encryption at rest (AES-256-GCM) — PRD-6
cryptography>=49.0.0
