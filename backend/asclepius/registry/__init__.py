"""International medical-registry verification.

``config`` says what a credential looks like in a given country; ``dispatch``
checks one against its registry; ``adapters`` holds the per-country lookups.
"""
