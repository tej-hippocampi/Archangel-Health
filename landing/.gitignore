.vercel

# Generated by the onboarding component tests (landing/test): a build of the
# production components plus a symlink to their dependencies. Rebuilt on every
# run; never edited by hand and never committed.
test/.build/
test/node_modules/
