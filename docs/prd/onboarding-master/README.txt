Hand Claude Code the whole folder. Start with 00_MASTER_ONBOARDING_PRD.md (it contains the prompt).
prds/A = applicant screen + exam blocker (this session)  ·  prds/B = form stability (audited: correct)
prds/C = CV extraction → review (audited: correct diagnoses; staged into phases 3 and 4)
evidence/ = the original JSDOM/CV investigation scripts — promote to tests, per the master.
