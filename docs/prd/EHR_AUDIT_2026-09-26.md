# ENV-EHR independent audit — 26 September 2026

A separate auditor reviewed the implementation read-only with repository
instructions and the PRD. The builder corrected findings; the auditor confirmed
the corrections. This is a software audit, not physician clinical acceptance.

## Design and checked scope

Reviews covered data preservation and legacy serialization; parser ambiguity and
PHI handling; temporal slicing and decoys; immutable sealed evidence; runtime
isolation; end-state grading and safety; reviewer assignment gates and blindness;
append-only verdict resolution and exactly-once earnings; package separation;
and evaluation reporting.

Final confirmations included inclusive age bounds and compatible decoy-role
selection; quote negation and destination-dose grounding; clinical PHI false
positives; outcome horizon, units, order, stop reasons and synthetic references;
realm-correct review links; single-plan outcome review with independent negative
confirmation; P4 reference withdrawal and export blocking; physician-approved
titration gates; and decoy-country provenance.

## Final tests and confirmation

The final auditor statement was:

> Independent read-only audit: all four findings are resolved; no remaining
> blockers in the checked scope. Confirmed unit-safe renal scoring and
> fingerprints, correct review earnings, final/provisional/withdrawn reporting
> separation, and rubric agreement stratified by version/provider/model/criterion.
> The empty rubric remains gated pending physician approval. 51 focused tests
> passed, including the fake-provider approval path. No files edited.

The final four corrections prevent non-mass doses reaching mg ceilings, include
outcome reference ratings in review quality checks, remove unfinished/withdrawn
rollouts from final metrics, and persist judge identity for rubric calibration.
Earlier confirmation runs included 131 related tests and legacy-model dump/JSON
comparisons on all twelve fixtures. Full application and container evidence is
recorded in `EHR_IMPLEMENTATION_STATUS.md`.

## Out of scope

The auditor did not approve a clinical rubric, source patient dataset, buyer
contract, or production release. The first real-data run and production store /
blob restoration remain operational acceptance requirements.

## Upload and agent qualification audit

A fresh read-only audit identified and confirmed fixes for public history
pagination, encounter date bounds, medication successor grading, calculator
error recovery, synthetic identity/date echoes, and provenance-checked repeated
history. The expanded upload test exposed narrative medication-list gaps;
additional adversarial checks covered exact drug/dose/frequency/route grounding,
negation, later regimen assertions and same-day structured-plan precedence.

Final independent report: “Confirmed both final fixes: continue events permit
baseline seeding, and smoke assertions require the correct drug and exact parsed
dose. Independent audit complete: no remaining findings in the reviewed scope.
Previously, 128 focused tests passed, including upload-to-export and
separate-grader replay; all 3 final regression tests also passed. git diff --check
is clean. Real-provider qualification remains for CI execution.”


## Real-provider qualification follow-up

The first CI real-provider qualification surfaced roleless OpenAI plain-message
transport, concatenated JSON actions, public argument ambiguity, and false note
consistency failures. The fix preserves async/sync conversation roles on both
OpenAI APIs, requests JSON-object output only for the JSON harness, rejects
multiple actions, and documents exact patient IDs and calculator input names.
The note checker accepts post-verbal orders, dated appointments, and ongoing
avoidance counseling while retaining negative/timing contradiction checks.

Local OCR now falls back to Tesseract/Poppler when OCRmyPDF is unavailable. A
real five-page synthetic scan preserves all five visit dates. Independent review
found reversed PDF bounds bypassing the pixel limit and three parser edge cases
(mixed-action timing, underscore identifiers, colonless Plan boundaries); each
received a targeted fix and regression. Shared provider/image regressions and
source-preservation inventory pass. Final confirmation and fresh CI evidence
are recorded in the PR after execution.

Final independent confirmation: "Confirmed both final fixes. All 26 grading tests
pass; git diff --check is clean. The NSAID class statement requires every active
member to have a persisted matching hold: zero or partial holds fail, complete
holds pass. The corrected referral regression also passes. No remaining findings
in this audited scope." Earlier follow-up review passed 169 tests with one opt-in
real-OCR skip and confirmed provider/native/image and OCR-bound compatibility.
